#include<bits/stdc++.h>
using namespace std;

struct TNode{
	TNode *child[2];
	TNode(){
		child[0] = NULL;
		child[1] = NULL;
	}
};

void solve(){
	
}

signed main()
{
	ios_base::sync_with_stdio(0);
	cin.tie(0);cout.tie(0);
	int t;
	cin>>t;
	while(t--){
		solve();
	}
}